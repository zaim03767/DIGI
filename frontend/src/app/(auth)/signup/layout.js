export const metadata = {
    title: "Sign In - Deep Fake Detection",
    description: "Sign in to your account",
  };
  
  export default function SigninLayout({ children }) {
    return <main>{children}</main>;
  }
  